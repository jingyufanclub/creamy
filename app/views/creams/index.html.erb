  <h1>MY SKIN CARE PRODUCTS</h1>
  <div class='ing_info'>
  &hearts; = favorite favorite

  
      <ol>
        <% @creams.each do |c| %>
        <li><%= link_to c.name, c %>
            <% if c.favorite %>
            &hearts;
            <% end %>
        </li>
        <% end %>
      </ol>
    </div>
    <div class='nav'>
    <%= link_to "New", new_cream_path, method: :get %><br>
    <%= link_to "Home", root_path %>
    </div>
