  <h1>My Skin Care Products</h1>
  <div class='ing_info'>
  &hearts; = <%= link_to "favorite favorite", :sort => "favorite DESC" %>
  &#9789; = <%= link_to "current rotation", :sort => "current_rotation DESC" %>
      <ol>
        <% @creams.each do |c| %>
        <li>
          <%= link_to c do %>
            <%= c.brand %>
            <%= c.name %>
          <% end%>
          <%= "#{c.fave}".html_safe %> <%= "#{c.rotation}".html_safe %>
        </li>
        <% end %>
      </ol>
    </div>
    <div class='nav'>
    <% if current_user %>
      <%= link_to "New", new_cream_path, method: :get %><br>
    <% end %>
    <%= link_to "Home", root_path %>
    </div>
